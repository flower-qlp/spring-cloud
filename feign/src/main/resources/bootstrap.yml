
server:
  port: 8765

spring:
  application:
    name: server-feign
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      profile: dev
      label: 20191202


#打开熔断
feign:
  hystrix:
    enabled: true
